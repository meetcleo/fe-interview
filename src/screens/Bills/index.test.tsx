import Bills from '.'

describe('Bills', () => {
  it.todo('shows bills that have the isBill = true flag')
  it.todo('calls the removeBill method when the primary button is clicked')
  it.todo('shows the error banner when an error is thrown')
})
