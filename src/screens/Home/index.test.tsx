import Home from '.'

describe('Home', () => {
  it.todo('shows a loading state when the app is initially hydrating')
  it.todo('shows an error state when the app has an error')
  it.todo('switches to the bills tab')
  it.todo('switches to the potential bills tab')
})
